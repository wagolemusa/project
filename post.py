def Post_contect():
	content = [
		{
			'user_id': 1,
			'post_id': 1,
			'title' : 'Testing exceptions',
			'conent': 'If we call div(0) in factorial.py we can see if raises an exception We can also test these exceptions, like'
		}
	]